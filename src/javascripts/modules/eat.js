import helpers from './universal-helpers';
// import data from './data';

// const eatVeggie = () => {
//   const activities = data.getActivities();

//   $('#button1-eat').click((divId) => {
//     activities.
//   })
// }

const eatDiv = (divId) => {
  helpers.displayDiv(divId);

  // eatVeggie(divId);
};

export default { eatDiv };
